# spatial-atac Python package

The Python half of the SpatialATAC workflow. Its only public function,
`place_atac_on_spots`, consumes the link and spot tables made by the companion
R package and returns a full-grid spatial ATAC `AnnData` object.

